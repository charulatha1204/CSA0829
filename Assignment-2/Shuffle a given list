import random

def shuffle_list(lst):
    random.shuffle(lst)
    return lst

# Example usage
my_list = [1, 2, 3, 4, 5]
shuffled_list = shuffle_list(my_list.copy())  # use copy() to keep original list unchanged
print("Shuffled list:", shuffled_list)
